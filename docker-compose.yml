version: '3'
services:
  web:
    build: .
    command: bundle exec rails s -p 4000 -b 0.0.0.0
    ports:
      - "4000:4000"
  # TODO for db services
  # db:
  #   image: mariadb
  #   volumes: 
  #     - ./dumps/db:/var/lib/mariadb/data