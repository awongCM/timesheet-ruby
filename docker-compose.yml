version: '3'
services:
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    ports:
      - "3000:3000"
  # TODO for db services
  # db:
  #   image: mariadb
  #   volumes: 
  #     - ./dumps/db:/var/lib/mariadb/data